name: ubuntu-20-04

on:
  push:
    branches:
    - master

jobs:
  android-build-container:
    runs-on: ubuntu-latest
   container:
     image: ubuntu:20.04

    - name: nproc
      id: nproc
      run: |
        nproc
