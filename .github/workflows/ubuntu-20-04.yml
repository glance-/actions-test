name: ubuntu-20-04

on:
  push:
    branches:
    - master

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: ubuntu:20.04

    steps:
    - name: nproc
      run: |
        nproc
