name: ubuntu-24-04

on:
  push:
    branches:
    - master

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: ubuntu:24.04

    steps:
    - name: nproc
      run: |
        nproc
