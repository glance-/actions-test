name: linux-debian-bookworm
on:
  push:
    branches:
    - master

jobs:
  setup-build:
    runs-on: ubuntu-latest
    container:
      image: debian:bookworm

    steps:
    - name: nproc
      run: |
        nproc
